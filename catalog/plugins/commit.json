{
  "name": "commit",
  "version": "0.3.0",
  "displayName": "Commit",
  "description": "Git commit, push, and pull-request workflows with a fixed safety floor: stage by name, refuse staged secrets, and — in the pull-request flow — never put a commit on the remote default branch.",
  "shortDescription": "Commit, push, and PR flows with a fixed safety floor.",
  "longDescription": "Commit mirrors the command surface of the official commit-commands plugin with four skills — commit, commit-push, commit-pr, and commit-clean — sharing one commit flow: stage related changes by name, refuse to commit secrets, and write a message in the repository's existing style that explains why. commit defaults to commit-only. commit-push commits and pushes the branch that is already checked out to its upstream, the default branch included, without opening a pull request. commit-pr never switches the working tree's branch and keeps the commit off the remote default branch; when invoked on the default branch it records the commit as a new local ref, pushes that ref, and opens the PR with an explicit head, optionally returning the local default branch to its remote state. commit-clean deletes only local branches whose upstream is marked gone, removing their linked worktrees first.",
  "author": {
    "name": "Shuaiqi Wang",
    "email": "wsq961@outlook.com",
    "url": "https://github.com/myWsq"
  },
  "homepage": "https://github.com/myWsq/plugins",
  "repository": "https://github.com/myWsq/plugins-builder",
  "license": "MIT",
  "keywords": [
    "git",
    "commit",
    "pull-request",
    "skills",
    "branch-cleanup"
  ],
  "capabilities": [
    "Skills",
    "Git"
  ],
  "defaultPrompt": [
    "Commit my changes.",
    "Commit, push, and open a PR.",
    "Clean up branches whose remote is gone."
  ],
  "brandColor": "#059669",
  "targets": {
    "claude": {
      "category": "development"
    },
    "codex": {
      "category": "Developer Tools",
      "policy": {
        "installation": "AVAILABLE",
        "authentication": "ON_INSTALL"
      }
    }
  }
}
