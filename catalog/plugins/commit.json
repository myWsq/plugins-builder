{
  "name": "commit",
  "version": "0.2.0",
  "displayName": "Commit",
  "description": "Git commit, push, and pull-request workflows with a fixed safety floor: stage by name, refuse staged secrets, and never put a commit on the remote default branch.",
  "shortDescription": "Commit, push, and PR flows with a fixed safety floor.",
  "longDescription": "Commit mirrors the command surface of the official commit-commands plugin with three skills — commit, commit-pr, and commit-clean — sharing one commit flow: stage related changes by name, refuse to commit secrets, and write a message in the repository's existing style that explains why. commit-pr additionally never switches the working tree's branch; when invoked on the default branch it records the commit as a new local ref, pushes that ref, and opens the PR with an explicit head, optionally returning the local default branch to its remote state. commit-clean deletes only local branches whose upstream is marked gone, removing their linked worktrees first.",
  "author": {
    "name": "Shuaiqi Wang",
    "email": "wsq961@outlook.com",
    "url": "https://github.com/myWsq"
  },
  "homepage": "https://github.com/myWsq/plugins",
  "repository": "https://github.com/myWsq/plugins-builder",
  "license": "MIT",
  "keywords": [
    "git",
    "commit",
    "pull-request",
    "skills",
    "branch-cleanup"
  ],
  "capabilities": [
    "Skills",
    "Git"
  ],
  "defaultPrompt": [
    "Commit my changes.",
    "Commit, push, and open a PR.",
    "Clean up branches whose remote is gone."
  ],
  "brandColor": "#059669",
  "targets": {
    "claude": {
      "category": "development"
    },
    "codex": {
      "category": "Developer Tools",
      "policy": {
        "installation": "AVAILABLE",
        "authentication": "ON_INSTALL"
      }
    }
  }
}
