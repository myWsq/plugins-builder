{
  "name": "@mywsq/plugins-builder",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node src/build.mjs",
    "check:release": "node src/check-release.mjs",
    "check:tag": "node src/check-tag.mjs",
    "sync:local": "node src/build.mjs && node src/sync-local.mjs",
    "test": "node --test",
    "verify": "npm test && npm run build"
  },
  "dependencies": {
    "semver": "^7.8.5"
  }
}
